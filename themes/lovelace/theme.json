{
    "$schema": "../theme.schema.json",
    "name": "Lovelace",
    "description": "A brutalist, nearly no-CSS theme that embraces browser defaults for maximum speed and simplicity.",
    "version": "1.0.0",
    "configuration": {
        "posts_per_page": {
            "type": "number",
            "default": 20,
            "header": "Posts per page",
            "description": "Number of posts to display per page"
        },
        "dark_mode": {
            "type": "boolean",
            "default": false,
            "header": "Dark mode",
            "description": "Match your reader's system dark mode preference"
        },
        "heading_placement": {
            "type": "select",
            "default": "header",
            "header": "Primary heading placement",
            "description": "Where the newsletter name should appear as the page's main heading.",
            "options": [
                { "id": "header", "label": "Header" },
                { "id": "content", "label": "Page content" }
            ]
        },
        "post_navigation": {
            "type": "boolean",
            "default": true,
            "header": "Post navigation",
            "description": "Show links to the previous and next post at the foot of each archived post"
        }
    }
}
